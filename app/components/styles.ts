import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    aspectRatio: 0.6,
  },
  contentContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
